import { ReactElement } from 'react';

import { {{pascalCase name}}Model } from 'models';

export interface I{{pascalCase name}}Context {
  text?: string;
}

export interface I{{pascalCase name}}Provider {
  children: ReactElement
}
